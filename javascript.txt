# 1️⃣ What is JavaScript?

## 🟢 Hinglish Explanation

JavaScript ek **high-level programming language** hai jo websites ko **interactive aur dynamic** banane ke liye use hoti hai.

Example:
- Button click handle karna  
- Form validation karna  
- Page reload kiye bina data fetch karna  
- Animations aur dynamic content show karna  

JavaScript browser ke andar run hoti hai.  
Backend me bhi use hoti hai using **Node.js**.

Simple samjho:
- HTML → Structure deta hai  
- CSS → Design deta hai  
- JavaScript → Logic aur behavior deta hai  

---

## 🌍 Real Life Example

Jab aap Facebook pe "Like" button click karte ho:
- Page reload nahi hota
- Like count instantly badh jata hai
- Ye sab JavaScript ke through hota hai

Amazon pe product search karte waqt suggestions real-time dikhti hain - ye bhi JavaScript hai.

---

## 🔄 Workflow

1. User webpage open karta hai
2. Browser HTML parse karta hai
3. CSS se styling apply hoti hai
4. JavaScript load hoti hai aur execute hoti hai
5. User interaction pe JavaScript events trigger hote hain
6. DOM update hota hai without page reload

---

## 🔵 English Explanation

JavaScript is a **high-level programming language** used to make websites **interactive and dynamic**.

Examples:
- Handling button clicks  
- Validating forms  
- Fetching data without reloading the page  
- Displaying dynamic content and animations  

JavaScript runs inside the browser.  
It can also be used on the backend using **Node.js**.

In simple terms:
- HTML → Provides structure  
- CSS → Provides styling  
- JavaScript → Provides logic and behavior  

---

## 🎯 Interview Answer

JavaScript is a high-level, interpreted programming language used to create interactive and dynamic web applications. It runs in the browser and can also be used on the backend using Node.js.

---

# 2️⃣ Is JavaScript synchronous or asynchronous?

## 🟢 Hinglish Explanation

JavaScript by default **synchronous** hoti hai.

Matlab:
Code line-by-line execute hota hai.  
Ek kaam khatam hone ke baad hi next kaam start hota hai.

Lekin JavaScript **asynchronous behavior support karti hai**:
- setTimeout()
- Promises
- async/await
- fetch()

Iska matlab hai:
Heavy task background me chalega aur baaki code rukega nahi.

---

## 🌍 Real Life Example

Restaurant me order karna:
- **Synchronous**: Ek customer ka order complete hone tak dusra customer wait kare (bahut slow!)
- **Asynchronous**: Sabke orders ek saath kitchen me jate hain, jiska banta hai wo serve ho jata hai

JavaScript bhi aise hi kaam karta hai - API calls background me process hoti hain jab tak aap page pe scroll kar sakte ho.

---

## 🔄 Workflow

**Synchronous:**
```
Task 1 → Complete → Task 2 → Complete → Task 3
```

**Asynchronous:**
```
Task 1 (start) → Task 2 (start) → Task 3 (start)
         ↓              ↓              ↓
    (background)   (background)   (background)
         ↓              ↓              ↓
     Complete       Complete       Complete
```

---

## 🔵 English Explanation

JavaScript is **synchronous by default**, meaning code executes line by line.

However, it supports **asynchronous behavior** using:
- setTimeout()
- Promises
- async/await
- fetch()

This allows long-running tasks to execute in the background without blocking the main thread.

---

## 🎯 Interview Answer

JavaScript is synchronous by default, but it supports asynchronous operations using callbacks, promises, and async/await to handle non-blocking tasks.

---

# 3️⃣ Is JavaScript single-threaded?

## 🟢 Hinglish Explanation

Haan, JavaScript **single-threaded** hai.

Iska matlab:
Ek time pe sirf ek hi task execute hota hai.

JavaScript ek **single call stack** use karti hai.

Lekin Event Loop ke through asynchronous tasks handle kiye jate hain.

---

## 🌍 Real Life Example

Ek cashier jo ek time pe sirf ek customer ko serve kar sakta hai - ye single-threaded hai.

Par wo token number de sakta hai aur jab product ready ho, customer ko call kar sakta hai - ye asynchronous behavior hai.

JavaScript bhi ek "thread" hai jo tasks ko organize karta hai efficiently.

---

## 🔄 Workflow

```
Single Thread (Call Stack)
        ↓
    Executing Task 1
        ↓
    Task 1 Complete
        ↓
    Executing Task 2
        ↓
    [Async Task 2 → Web API]
        ↓
    Executing Task 3
        ↓
    [Async Task Complete → Queue → Execute]
```

---

## 🔵 English Explanation

Yes, JavaScript is **single-threaded**, meaning it executes one task at a time.

It uses a single call stack.

Asynchronous tasks are handled using the **Event Loop**.

---

## 🎯 Interview Answer

Yes, JavaScript is single-threaded. It uses a single call stack and handles asynchronous operations using the event loop.

---

# 4️⃣ What are the data types in JavaScript?

## 🟢 Hinglish Explanation

JavaScript me 2 types ke data types hote hain:

### 🔹 Primitive Data Types
- **String** - Text data → "Hello"
- **Number** - Numeric values → 42, 3.14
- **Boolean** - True/False → true, false
- **Undefined** - Value assign nahi hui → let x;
- **Null** - Intentionally empty → let y = null;
- **BigInt** - Very large numbers → 9007199254740991n
- **Symbol** - Unique identifiers → Symbol('id')

### 🔹 Non-Primitive (Reference Type)
- **Object** - Key-value pairs → {name: "Sumit"}
- **Array** - Ordered list → [1, 2, 3]
- **Function** - Reusable code block

---

## 🌍 Real Life Example

**Primitive Types:**
- String: Kisi ka naam → "Sumit Samadhiya"
- Number: Age → 23
- Boolean: Is Adult? → true

**Reference Types:**
- Object: Student ki puri information → {name: "Sumit", age: 23, city: "Gwalior"}
- Array: Marks list → [85, 90, 78]
- Function: Percentage calculate karna → function calcPercentage() {...}

---

## 🔄 Workflow

**Primitive:**
```
Variable → Value directly stored in Stack Memory
let age = 23
```

**Reference:**
```
Variable → Reference stored in Stack → Actual Object in Heap Memory
let person = {name: "Sumit"}
```

---

## 🔵 English Explanation

JavaScript has two types of data types:

### 🔹 Primitive Data Types
- String
- Number
- Boolean
- Undefined
- Null
- BigInt
- Symbol

### 🔹 Non-Primitive (Reference Type)
- Object
- Array
- Function

---

## 🎯 Interview Answer

JavaScript has two categories of data types: primitive types like string, number, boolean, null, undefined, symbol, and BigInt, and non-primitive types like objects and arrays.

---

# 5️⃣ Difference between var, let, and const

## 🟢 Hinglish Explanation

### 🔹 var
- Function scoped
- Hoisted hota hai
- Re-declare aur re-assign dono kar sakte hain

### 🔹 let
- Block scoped
- Hoisted hota hai but TDZ me rehta hai
- Re-assign kar sakte hain, re-declare nahi

### 🔹 const
- Block scoped
- Value change nahi kar sakte
- Re-declare nahi kar sakte

---

## 🌍 Real Life Example

**var** - Purana zamana, poore ghar me koi bhi use kar sakta hai
```javascript
var oldPhone = "Nokia";
var oldPhone = "Samsung"; // ✅ Allowed
```

**let** - Modern, sirf apne kamre me use kar sakte ho
```javascript
let myPhone = "iPhone";
myPhone = "Samsung"; // ✅ Allowed
let myPhone = "OnePlus"; // ❌ Error
```

**const** - Permanent, jaise Aadhaar card number change nahi hota
```javascript
const aadhaar = "1234-5678-9012";
audhaar = "new number"; // ❌ Error
```

---

## 🔄 Workflow

```
var → Function Scope → Hoisted (undefined) → Re-declarable

let → Block Scope → Hoisted (TDZ) → Not Re-declarable

const → Block Scope → Hoisted (TDZ) → Not Re-declarable, Not Re-assignable
```

---

## 🔵 English Explanation

### 🔹 var
- Function scoped
- Hoisted
- Can be re-declared and re-assigned

### 🔹 let
- Block scoped
- Hoisted but in temporal dead zone
- Can be re-assigned but not re-declared

### 🔹 const
- Block scoped
- Cannot be re-assigned
- Cannot be re-declared

---

## 🎯 Interview Answer

The main difference is that var is function-scoped, while let and const are block-scoped. Let allows reassignment but const does not.

---

# 5️⃣ Additional Questions will be added soon...